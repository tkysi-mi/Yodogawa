# リサーチ: task000001-sample-feature

`d-001-review-retrospective` のドッグフーディング用サンプルタスク。

## ベストプラクティス／参考リンク

- トグルコンポーネントは既存の `src/components/Toggle.tsx` を再利用する。

## 既存コード調査

- `src/pages/SettingsPage.tsx` に既存の設定項目がある。パターンを踏襲する。

## 技術選定

- 追加ライブラリ無し。既存コンポーネントで実現可能。

## 技術的リスク

- 通知設定APIのレスポンス形式が未確定（低リスク・要確認）。

## 実装時に発見したベストプラクティス

- 設定変更APIは楽観的UI更新（optimistic update）にすると体感速度が上がる。

## 技術的リスクの結果

- レスポンス形式は既存の `/api/settings` と同一で問題なかった。
